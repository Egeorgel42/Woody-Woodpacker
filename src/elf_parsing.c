#include "woody.h"

/*
void	parse_elf(int fd)
{
	unsigned char	e_ident[EI_NIDENT];
	unsigned char	check_ver[5] = {0x7c, 'E', 'L', 'F', 1};

	ssize_t rd = read(fd, e_ident, EI_NIDENT);

	ft_strncmp(e_ident, check_ver, 5);
}*/