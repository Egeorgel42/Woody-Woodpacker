#include "woody.h"

void	payload_insert32(parsing_info *info, char *file_buf, char *payload, size_t payload_size, char **err_msg)
{
	(void)info;
	(void)file_buf;
	(void)payload;
	(void)payload_size;
	(void)err_msg;
}